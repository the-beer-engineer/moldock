# Network: regtest | testnet | mainnet
NETWORK=regtest

# Fee rate in sats/kB (miners accept 3-100; use lowest that works)
FEE_RATE_SATS_PER_KB=10

# Dispatch agent private key (hex). If empty, generates a new one each run.
DISPATCH_PRIVATE_KEY=

# Server port
PORT=3456
